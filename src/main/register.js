import React from 'react';
import Register from '../Components/crud/Register';

const register = () => {
    return (
        <div>
            <Register />
        </div>
    );
};

export default register;